const MESSAGES_ERRORS ={
    required: 'The field is require',
    pattern: 'The format is incorrect',
    minlength: 'The minimum length #val',
    maxlength: 'The maximum length #val',
};

export default MESSAGES_ERRORS;